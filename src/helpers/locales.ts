import { LocaleCollection } from 'react-locale-hoc';

import enUS from '../assets/locales/en-US.json';
import fr from '../assets/locales/fr.json';

const locales: LocaleCollection = {
    'en-US': enUS,
    fr: fr,
};

export default locales;
